import 'package:flutter/material.dart';

class StudyGroupFinderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Study Group Finder Options'),
      ),
      body: Center(
        child: Text('Study Group Finder Content'),
      ),
    );
  }
}